const fetch = require('node-fetch');


module.exports.load = async function(app, ifValidAPI, ejs) {
    app.get("/servers", async (req, res) => {
        
    });
};

module.exports.load = async function(app, ifValidAPI, ejs) {
    app.get("/servers/create?", async (req, res) => {
        
    });
};